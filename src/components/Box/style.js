import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  boxContainer: {
    width: 200,
    height: 200,
    backgroundColor: "blue",
    borderRadius:10,
    zIndex:10,
    margin:5,
  },
});
